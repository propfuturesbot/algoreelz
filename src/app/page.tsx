import { ReelsContainer } from '@/components/ReelsContainer';

export default function Home() {
  return (
    <main className="w-full h-screen overflow-hidden bg-bg-primary">
      <ReelsContainer />
    </main>
  );
}
